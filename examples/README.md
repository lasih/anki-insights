# Examples

Run the offline dedup example which uses a packaged fixture:

```bash
python -m examples.dedup_example
```

The example writes `examples/output/anki_dedup_example.csv` and demonstrates using the library
without AnkiConnect.
